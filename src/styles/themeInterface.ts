export interface ThemeProps {
  colors: {
    white: string;
    accentDark: string;
    primaryGreen: string;
    secondaryGreen: string;
    textLight: string;
  };

  sizes: {
    containerL: string;
  };
}

export interface ThemeProps {
  colors: {
    white: string;
    black: string;
    grey: string;
    accentDark: string;
    primaryGreen: string;
    secondaryGreen: string;
    textLight: string;
  };

  sizes: {
    containerL: string;
  };
}
